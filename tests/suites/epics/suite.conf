# epics.mjs — which epics are open, across every owner the account reaches.
#
# Both CLIs are stubbed: every question here is a forge's. A request carries what it asked
# for — the host, the search query, the listing path — and a case naming what it expects
# refuses anything else, so a query that dropped its host or its scope fails the case rather
# than looking right. A call nobody planned for exits 1.
script plugins/hcb-dev/scripts/epics.mjs
