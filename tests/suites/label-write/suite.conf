# label-write.mjs — labels put on and taken off one carrier, read back.
#
# Both CLIs are stubbed and keep the carrier's labels in a store beside the marker, so a write
# changes what the next read answers. A name arrives in a JSON file and must leave in a JSON body:
# a case whose name carries shell or a comma fails if the name reached an argv flag instead.
script plugins/hcb-dev/scripts/label-write.mjs
