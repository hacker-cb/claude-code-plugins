# issue-slice.mjs — every issue of a slice with its links, one call per page, and whether
# what came back is the whole slice.
#
# Both CLIs are stubbed outright, since every question here would otherwise go to a real
# forge about the runner's own checkout. A GraphQL request is answered by the envelope
# entry whose `match` equals the request's own variables and whose `has` is in its query
# text — a filter, a cursor or a milestone that never reached the request finds no answer,
# and the case fails on it. The output is JSONL written compactly, so the fragments below
# are spelled without spaces.
script plugins/hcb-dev/scripts/issue-slice.mjs
