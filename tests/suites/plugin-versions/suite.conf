# What this suite runs, and the environment it runs in. Everything else a suite
# needs is the directory around this file: `cases.tsv`, `fixtures/`, `stub/`.
#
# One `key value` per line; `env` may repeat. Paths are relative to the repo root.

script	plugins/hcb-dev/skills/session-plugin-refresh/scripts/plugin-versions.mjs

# No shared `args`: every case names the plugin root it reads, because the root is
# what each one is about — a cache directory, a working copy, or nothing at all.
