# pr-state.mjs — what the forge says about one change request, and what is outstanding.
#
# Both CLIs are stubbed outright: every question here would otherwise reach a network. The
# review threads come from a GraphQL query rather than a `pr view` field, so the `gh` stub
# answers the two separately — a request whose view reads and whose threads do not is the
# case the whole `threads.read` flag exists for.
script plugins/hcb-dev/scripts/pr-state.mjs
