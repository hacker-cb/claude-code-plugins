# copilot-findings.mjs — what the automated reviewer said on one request, and what of it
# is still unanswered.
#
# `gh` is stubbed outright: every question here is a forge's, and a stub with no answer
# for a call exits 3 so a request nobody planned for fails the case rather than reaching
# the network.
script plugins/hcb-dev/scripts/copilot-findings.mjs
