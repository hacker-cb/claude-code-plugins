# landings.mjs — what the base took between two of its commits, as landings.
#
# git and both forge CLIs are stubbed outright: every question here would otherwise answer
# about the runner's own checkout and its forge. The envelope holds the range's commits,
# oldest first, and what each forge says carries each one.
script plugins/hcb-dev/skills/master-session/scripts/landings.mjs
