# resolve-base.mjs — which remote to read from, which to push to, and whether a base is a
# current ref.
#
# All three CLIs are stubbed outright: every question is about a remote or about a
# checkout the runner does not have, and a stub with no answer exits 3 so an unplanned
# call fails the case rather than reaching a network.
script plugins/hcb-dev/scripts/resolve-base.mjs
