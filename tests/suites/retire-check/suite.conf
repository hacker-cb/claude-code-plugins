# retire-check.mjs — is this branch safe to retire, and on which side.
#
# `git` is a proxy — real except for the questions that would reach a network or read
# the runner's own checkout shape — and `gh` is stubbed outright. The two sides are
# checked separately because they fail separately, and most of these cases exist to
# prove one stays safe while the other does not.
script plugins/hcb-dev/scripts/retire-check.mjs
