# branch-publish.mjs — the name a branch ships under, put on the remote, and the names it
# used to carry taken off it.
#
# Both CLIs are stubbed outright rather than proxied: every question here is about a
# remote, or about a checkout this branch is not standing in, and a case resting on the
# runner's own clone would test the clone. A stub with no answer for a call exits 3, so a
# command nobody planned for fails the case instead of reaching the real git.
script plugins/hcb-dev/scripts/branch-publish.mjs
