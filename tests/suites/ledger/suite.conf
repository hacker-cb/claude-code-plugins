# ledger.mjs — where an epic's wave ledger lives, whether the next write fits, and what
# archives stand beside it.
#
# Both CLIs are stubbed: every question here is a forge's. A stub with no answer for a call
# exits 1 so a request nobody planned for fails the case rather than reaching the network.
script plugins/hcb-dev/scripts/ledger.mjs
