# cleanup-scan.mjs — what a sweep would find, and what each thing carries.
#
# Both CLIs are stubbed outright: every question here would otherwise answer about the
# runner's own checkout. `for-each-ref` is asked for a NUL-separated format, and a JSON
# envelope cannot hold a NUL — so the fixtures write U+0001 where one belongs and the
# stub translates. Neither character can appear in a branch name, which is what makes one
# stand in for the other.
script plugins/hcb-dev/scripts/cleanup-scan.mjs
