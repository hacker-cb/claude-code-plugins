# copilot-state.mjs — what Copilot's state is on a pull request's CURRENT head.
#
# `gh` is stubbed outright: every call reaches a forge, so nothing may fall through.
# The captured envelopes come from a real pull request whose head carried NO Copilot
# review while six of them stood on earlier commits — which is the exact shape the
# script exists to tell apart, and not one anybody would have invented.
script plugins/hcb-dev/scripts/copilot-state.mjs
