# labels.mjs — the label set and its carriers read whole, and a plan checked against them.
#
# The CLIs are stubbed: `snapshot` reads GitHub's GraphQL pages and REST listings, GitLab's REST
# listings, from the envelope. The checks read snapshot files kept in snaps/ and call nothing.
script plugins/hcb-dev/skills/label-taxonomy/scripts/labels.mjs
